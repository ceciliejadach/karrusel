---
import "../styles/generel.css";
---

<form name="newsletter" netlify>
  <h2>Newsletter</h2>
  <p>Sign up for our newsletter and receive notifications when there are updates and new names are announced.</p>
  <input type="text" placeholder="Your e-mail address here..." />
  <div class="submit_button">
    <input type="submit" value="Sign up" />
  </div>
  <p class="small">P.S.: By signing up, you subscribe to receive our newsletter. You can always unsubscribe. View our privacy policy here.</p>
</form>

<style>
  form h2 {
    margin-bottom: 15px;
    text-align: center;
  }

  form p {
    text-align: center;
  }
  .small {
    font-size: var(--font-size-p-small);
  }

  form {
    max-width: 300px;
    margin: 20px auto 60px;
  }
  input[type="text"] {
    width: 100%;
    padding: 12px;
    border: 1px solid #ccc;
    border-radius: 5px;
    margin: 15px 0 10px;
  }
  input[type="submit"] {
    padding: 0.7rem 1.5rem;
    border-radius: 10px;
    font-size: var(---font-size-p);
    border: solid 1px var(---text-color);
    cursor: pointer;
    display: block;
    background-color: transparent;
    color: var(---text-color);
    margin: 0px auto 15px;
  }

  input[type="submit"]:hover {
    background-color: var(---green-100);
    color: var(---background-color);
    border-color: var(---background-color);
    transition: 0.3s;
  }

  @media (min-width: 800px) {
    form {
      margin: 0;
    }
  }
</style>
